name: bu
root: ~/Projects/betterup-app

pre: echo 'Starting virtual machine' && bin/setup && eval $(docker-machine env dev)

windows:
  - editor: ee
  - guard: bin/guard -c
  - bash: ls
  - logs: docker-compose logs
