name: bu
root: ~/Projects/betterup-app

pre: echo 'Starting vagrant machine' && vagrant-global-shutdown && bin/setup

windows:
  - editor: vim .
  - guard: bin/guard --clear
  - logs: tail -f log/development.log
  - bash: ls
